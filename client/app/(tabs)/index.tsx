import { Text, View } from 'react-native';

const Index = () => {
    return (
        <View className="h-full flex-col items-center justify-center">
            <Text className="text-3xl text-red-500">Dashboard</Text>
        </View>
    );
};

export default Index;
